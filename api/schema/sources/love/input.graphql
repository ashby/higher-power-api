#import VulnerabilityInput from '../../feelings/vulnerability/input.graphql';
#import AcceptanceInput from '../../feelings/acceptance/input.graphql';

scalar DateTime

input LoveInput {
    id: ID
    feeling: String
    feelingId: ID
    vulnerability: [VulnerabilityInput]
    acceptance: [AcceptanceInput]
    createdAt: DateTime
    updatedAt: DateTime
}