#import Armor from '../../feelings/armor/type.graphql';

scalar DateTime

type Denial {
    id: ID!
    feeling: String
    feelingId: ID
    armor: [Armor]
    createdAt: DateTime
    updatedAt: DateTime
}