#import BoundaryInput from '../../feelings/boundary/input.graphql';

scalar DateTime

input TowerInput {
    id: ID
    feeling: String
    feelingId: ID
    boundary: [BoundaryInput]
    createdAt: DateTime
    updatedAt: DateTime
}