#import Armor from '../../feelings/armor/type.graphql';

scalar DateTime

type Sarcasm {
    id: ID!
    feeling: String
    feelingId: ID
    armor: [Armor]
    createdAt: DateTime
    updatedAt: DateTime
}