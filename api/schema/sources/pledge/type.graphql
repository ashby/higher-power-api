#import Loyalty from '../../feelings/loyalty/type.graphql';
#import Honor from '../../feelings/honor/type.graphql';

scalar DateTime

type Pledge {
    id: ID!
    feeling: String
    feelingId: ID
    loyalty: [Loyalty]
    honor: [Honor]
    createdAt: DateTime
    updatedAt: DateTime
}